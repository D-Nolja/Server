<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dang.dnolja.user.model.mapper.UserMapper">


    <insert id="join" parameterType="userDto">
        insert into user (password, nick_name, email, profile,  verified)
        values (#{password}, #{username}, #{email}, #{profile}, 0)
    </insert>


    <select id="findByEmail" parameterType="String" resultType="com.dang.dnolja.user.model.dto.UserDto">
        select user_id, nick_name, email, password, roles, provider_id, provider, profile, status, created_at, modified_at, verified
        from User
        where useremail = #{email}
    </select>

    <update id="updateVerified" parameterType="long">
        update User
        set verified = 1
        where user_id = #{userId}
    </update>

</mapper>